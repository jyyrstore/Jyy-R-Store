const repo=require('../../repositories/settings.repository'); async function publicSettings(){return repo.getPublic();} async function ownerSettings(){return repo.getAll();} async function save(items){for(const item of items) await repo.set(item.key,item.category,item.value_json,!!item.is_public); return repo.getAll();} module.exports={publicSettings,ownerSettings,save,maintenance:repo.maintenance,setMaintenance:repo.setMaintenance};
