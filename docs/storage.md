# Storage

Documents planned storage boundaries, bucket configuration, upload validation, object naming, privacy, retention, and access control. Public and private content must be deliberately separated.

Storage credentials/configuration will be supplied through environment settings and must not appear in source.
