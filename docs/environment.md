# Environment

Documents runtime configuration principles. Environment names are declared in `.env.example`; the actual `.env` is intentionally empty. Secrets must be injected through secure environment configuration and validated at application start once implementation exists.
